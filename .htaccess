RewriteEngine On

# Redirect old /index.html requests to the clean root URL
RewriteCond %{THE_REQUEST} \s/index\.html\s [NC]
RewriteRule ^index\.html$ / [R=301,L]

# Send requests to the home page when the directory root is requested
DirectoryIndex index.html
